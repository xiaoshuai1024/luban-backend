name: Test

on:
  pull_request:
    branches: [master, main]
  push:
    branches: [master, main]

permissions:
  contents: read

jobs:
  test:
    name: Verify (mvn verify + ArchUnit + JaCoCo)
    runs-on: ubuntu-latest
    # H2（test profile）+ 无外部 MySQL/Redis。
    # LeadServiceIntegrationTest @Disabled（需 Testcontainers MySQL + Redis，CI 不满足）。
    services: {}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: mvn verify (含 ArchUnit 架构测试, test profile, H2)
        run: mvn -B -q verify

      - name: Generate JaCoCo coverage report
        run: mvn -B jacoco:report

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: java-coverage-report
          path: target/site/jacoco/
